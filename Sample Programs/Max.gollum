
~~ Returns the largest element in an array.

 magic it max (a) {
    it largest = a[0]
    revolves (it i = 1; i <  a.thingsInIt; i ++) {
        ifes (a[i] > largest) {
            largest = a[i]
        }
    }
    givesUs largest
GollumGollum